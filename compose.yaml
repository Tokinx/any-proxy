services:
  any-proxy:
    build:
      context: .
    image: any-proxy:local
    container_name: any-proxy
    restart: unless-stopped
    environment:
      PORT: 3000
      ALLOWLIST: ""
      WS_QUEUE_LIMIT_BYTES: 1048576
    ports:
      - "3000:3000"
