services:
  any-proxy:
    build:
      context: .
    image: any-proxy:local
    container_name: any-proxy
    restart: unless-stopped
    environment:
      PORT: 3000
    ports:
      - "3000:3000"
